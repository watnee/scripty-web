{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "Scripty",
      "runtimeExecutable": "bash",
      "runtimeArgs": ["-c", "export JAVA_HOME=$(/usr/libexec/java_home -v 17) && export PATH=$JAVA_HOME/bin:$PATH && mvn spring-boot:run -Dspring-boot.run.profiles=dev -Dspring-boot.run.arguments=--server.port=${PORT:-8080}"],
      "port": 8080,
      "autoPort": true
    }
  ]
}
