# Keep railway up uploads small — Railway builds from source on their side.
# Root-anchor directory patterns (leading /) — a bare "db/" matches at ANY
# depth, which silently excluded src/main/resources/db/ (all Flyway
# migrations) from railway up builds.
.git/
.github/
.agents/
.claude/
.railway/
node_modules/
package.json
package-lock.json
target/
/tmp/
/uploads/
/db/
*.log
*.pid
*.command
.reloadtrigger
hs_err_pid*
scripty-server*
