This directory acts as the local file system config repository for the Spring Cloud Config Server.
Properties files placed here will override the ones in classpath:/config-repo without requiring a project rebuild.
