{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile",
    "watchPatterns": [
      "src/**",
      "pom.xml",
      "Dockerfile",
      "railway.json"
    ]
  },
  "deploy": {
    "startCommand": "java -XX:MaxRAMPercentage=75.0 -jar scripty.jar --server.port=$PORT --spring.profiles.active=prod",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
