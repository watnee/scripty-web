[# th:if="${firstName}"]Hello [(${firstName})],[/][# th:unless="${firstName}"]Hello,[/]

Someone asked to reset the password for the Scripty account at [(${email})].

Open this link to choose a new one. On a device with the Scripty app installed
it opens the app; anywhere else it opens the same page in your browser.

[(${resetUrl})]

The link stops working in [(${expiryHours})] hours.

If this wasn't you, ignore this email. Your password stays as it is, and the
link expires on its own.

--
Sent by Scripty. This is an automated message - replies to it are not read.
